using UnityEngine;
using System.Collections;

public class Hero : MonoBehaviour {

    public float health;
    public float moveSpeed;
    public Habilidades[] habs;

	void Start () {
	    
	}
	
	void Update () {
        //if (Input.GetMouseButtonDown(0)) habs[0].Use();
        //if (Input.GetKeyDown(KeyCode.Space)) habs[1].Use();
        //if (Input.GetKeyDown(KeyCode.R)) habs[2].Use();
        //if (Input.GetKeyDown(KeyCode.Q)) habs[3].Use();
        //if (Input.GetKeyDown(KeyCode.E)) habs[4].Use();
	}
}
